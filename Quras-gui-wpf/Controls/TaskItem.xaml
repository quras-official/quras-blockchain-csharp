<UserControl x:Class="Quras_gui_wpf.Controls.TaskItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Quras_gui_wpf.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="250">
    <Grid Margin="5,3,5,3">
        <Border x:Name="bdTask" CornerRadius="3" BorderThickness="1,1,1,1" BorderBrush="YellowGreen">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="tbTime" Margin="15,5,5,5" Grid.Row="0" FontSize="10" Foreground="YellowGreen" TextAlignment="Left" FontWeight="Normal" FontFamily="SF Pro Display">
                    2019/4/7 24:32:54
                </TextBlock>

                <TextBlock x:Name="tbMessage" Grid.Row="1" Margin="15,0,5,5" FontSize="10" Foreground="LightGray" TextAlignment="Left" FontWeight="Normal" FontFamily="SF Pro Display">
                    Messages <LineBreak/>
                    ZK-SNARKS PK loading
                </TextBlock>
            </Grid>
        </Border>
    </Grid>
</UserControl>
